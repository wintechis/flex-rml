#ifndef DEFINITIONS_H
#define DEFINITIONS_H

#include <string>
#include <vector>

// Strcut to hold a Triple
struct NTriple {
  std::string subject;
  std::string predicate;
  std::string object;
};

#endif